{
  "name": "kit-flaky-waits",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "install:browsers": "npx playwright install --with-deps chromium",
    "start": "node app/server.js",
    "test": "npx playwright test",
    "run:A": "node tools/run-repeated.mjs --treatment A --runs 50 --subject S01",
    "run:B": "node tools/run-repeated.mjs --treatment B --runs 50 --subject S01"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
